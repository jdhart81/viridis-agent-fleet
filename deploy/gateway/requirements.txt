# Viridis Agent Stable gateway direct dependencies.
# Exact transitive production resolution lives in requirements.lock and is
# the only file installed by the production Dockerfile.
mcp==1.28.1
uvicorn==0.51.0
starlette==1.3.1
cryptography==49.0.0
