# agent-identity-registry-agent
Verifiable identity + capability discovery for the agent-to-agent economy. See [AGENT.md](AGENT.md).
Novel agent added 2026-07-09 (**identity** → trust → escrow). The registry hub the
other two A2A agents plug into. Stdlib-only core; MCP-first.
Run tests: `PYTHONPATH=. pytest tests/ --override-ini=asyncio_mode=auto`
