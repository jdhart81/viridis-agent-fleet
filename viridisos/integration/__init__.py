"""ViridisOS × Agent Fleet integration layer — one root / one mark / one toll.

Bones for external handoff (Sol). Implement trust_root.py, mark.py, toll.py so
`python3 -m pytest integration/tests -q` passes. See BUILD_SPEC.md. Additive only.
"""
